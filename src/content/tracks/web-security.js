import { createProfessionalTrack } from "../builders/createProfessionalTrack.js";
import { webSecurityModules } from "./webSecurityModules.js";

const v = {
  threat: ["modèle de menace", "threat model", "Analyse des actifs, acteurs, frontières, abus et protections.", "Analysis of assets, actors, boundaries, abuse cases, and controls."],
  xss: ["XSS", "XSS", "Injection de contenu actif exécuté dans le contexte d’un site.", "Injection of active content executed in a site's context."],
  csrf: ["CSRF", "CSRF", "Action forcée via la session existante d’un utilisateur.", "An action forced through a user's existing session."],
  validation: ["validation", "validation", "Vérification stricte des données avant leur usage.", "Strict checking of data before use."],
  authentication: ["authentification", "authentication", "Preuve de l’identité d’un acteur.", "Proof of an actor's identity."],
  authorization: ["autorisation", "authorization", "Décision indiquant si une identité peut effectuer une action.", "A decision about whether an identity may perform an action."],
  session: ["session", "session", "État serveur ou jeton reliant des requêtes à une identité authentifiée.", "Server state or token linking requests to an authenticated identity."],
  leastPrivilege: ["moindre privilège", "least privilege", "Accorder seulement les permissions nécessaires, pour la durée nécessaire.", "Grant only required permissions, for only the required time."],
  secret: ["secret", "secret", "Valeur confidentielle donnant accès à un système ou une capacité.", "A confidential value granting access to a system or capability."],
  csp: ["CSP", "CSP", "Politique navigateur limitant les sources de contenu exécutable.", "A browser policy limiting executable content sources."],
  cors: ["CORS", "CORS", "Politique serveur autorisant explicitement certaines origines navigateur.", "A server policy explicitly allowing selected browser origins."],
  upload: ["upload", "upload", "Entrée binaire non fiable nécessitant taille, type, nom et stockage contrôlés.", "Untrusted binary input requiring controlled size, type, name, and storage."],
  observability: ["observabilité", "observability", "Capacité à comprendre un système par des signaux sûrs et corrélés.", "The ability to understand a system through safe, correlated signals."],
  incident: ["incident", "incident", "Événement de sécurité nécessitant détection, confinement, correction et retour d’expérience.", "A security event requiring detection, containment, remediation, and learning."]
};

export const webSecurityTrack = createProfessionalTrack({
  id: "web-security",
  label: "SEC",
  title: ["Sécurité web appliquée", "Applied web security"],
  summary: ["Identifie les menaces et corrige validation, XSS, CSRF, sessions, autorisation, secrets, headers, uploads et incidents.", "Identify threats and fix validation, XSS, CSRF, sessions, authorization, secrets, headers, uploads, and incidents."],
  profession: ["La sécurité est un ensemble de décisions vérifiables réparties dans l’architecture. Le parcours part des actifs et abus réels, applique des défenses en profondeur et exige des tests négatifs plutôt qu’une simple checklist.", "Security is a set of verifiable decisions distributed across the architecture. This track starts from real assets and abuse cases, applies defense in depth, and requires negative tests rather than a simple checklist."],
  prerequisites: [["Comprendre HTTP, navigateur et API", "Understand HTTP, browsers, and APIs"], ["Connaître Node.js et SQL", "Know Node.js and SQL"], ["Savoir écrire des tests", "Know how to write tests"]],
  outcomes: [["Construire un modèle de menace", "Build a threat model"], ["Prévenir injections et actions forcées", "Prevent injections and forced actions"], ["Sécuriser identité, secrets et accès", "Secure identity, secrets, and access"], ["Auditer et répondre à un incident", "Audit and respond to an incident"]],
  capstone: ["Auditer une application complète, prioriser les risques, corriger les frontières et livrer preuves, tests et procédure d’incident.", "Audit a complete application, prioritize risks, fix boundaries, and ship evidence, tests, and an incident procedure."],
  certification: [["Valider chaque module", "Pass every module"], ["Livrer quatre audits/corrections", "Ship four audits/remediations"], ["Réussir l’examen final", "Pass the final exam"], ["Faire approuver les tests d’abus", "Get abuse-case tests approved"]],
  modules: [
    ...webSecurityModules,
    {
      id: "security-threats-input",
      title: ["Menaces, validation et navigateur", "Threats, validation, and browser"],
      description: ["Partir des actifs et frontières puis neutraliser les entrées actives et actions forcées.", "Start from assets and boundaries, then neutralize active input and forced actions."],
      vocabulary: [v.threat, v.xss, v.csrf],
      lessons: [
        text("sec-01-threat-model", ["Construire un modèle de menace", "Build a threat model"], ["Cartographie actifs, acteurs, frontières, abus, impact et protections.", "Map assets, actors, boundaries, abuse cases, impact, and controls."], ["Actifs : comptes, progression, certificats, clés", "Acteurs : apprenant, auteur, admin, attaquant", "Frontières : navigateur, API, Supabase, email", "Abus : accès croisé, publication non autorisée, vol de session", "Impact et preuves de contrôle"], ["threat-modeling"], [v.threat, v.leastPrivilege]),
        code("sec-01-validation", ["Valider une entrée non fiable", "Validate untrusted input"], ["Parse un payload strict, limite longueur, enum, protocole URL et champs inconnus.", "Parse a strict payload and limit length, enum, URL protocol, and unknown fields."], "function isHttpUrl(value) {\n  try { return ['https:', 'http:'].includes(new URL(value).protocol); }\n  catch { return false; }\n}\nconst httpUrl = z.string().url().max(500).refine(isHttpUrl);\nconst submissionSchema = z.object({\n  projectId: z.string().uuid(),\n  repositoryUrl: httpUrl,\n  note: z.string().trim().max(2000),\n  visibility: z.enum(['private', 'reviewers'])\n}).strict();\n\nconst result = submissionSchema.safeParse(request.body);\nif (!result.success) return response.status(400).json({ error: { code: 'VALIDATION_ERROR', issues: result.error.issues } });\nrequest.body = result.data;", ["z.object", "uuid()", "url()", "max(", "refine", "https:", "http:", "try", "catch", "z.enum", ".strict()", "safeParse", "status(400)", "request.body = result.data"], ["input-validation", "mass-assignment"], [v.threat, ["liste blanche", "allowlist", "Ensemble fermé de valeurs explicitement acceptées.", "A closed set of explicitly accepted values."]]),
        code("sec-01-xss-csrf", ["Prévenir XSS et CSRF", "Prevent XSS and CSRF"], ["Rends le texte comme donnée, configure cookie et vérifie origine plus jeton sur mutation.", "Render text as data, configure the cookie, and verify origin plus token on mutations."], "app.use(session({ cookie: { httpOnly: true, secure: true, sameSite: 'lax' } }));\n\nfunction requireCsrf(request, response, next) {\n  if (!allowedOrigins.includes(request.headers.origin)) return response.status(403).json({ error: { code: 'ORIGIN_DENIED' } });\n  if (!timingSafeEqualToken(request.session.csrfToken, request.headers['x-csrf-token'])) return response.status(403).json({ error: { code: 'CSRF_DENIED' } });\n  next();\n}\n\nfunction Comment({ body }) { return <p>{body}</p>; }", ["httpOnly: true", "secure: true", "sameSite: 'lax'", "allowedOrigins.includes", "x-csrf-token", "status(403)", "<p>{body}</p>"], ["xss-prevention", "csrf-prevention", "secure-cookies"], [v.xss, v.csrf, v.session]),
        project("sec-01-boundary-project", ["Mini-projet : sécuriser un formulaire public", "Mini-project: secure a public form"], ["Ajoute validation, encodage, CSRF, limites et tests de payload malveillant.", "Add validation, encoding, CSRF, limits, and malicious-payload tests."], "app.use(express.json({ limit: '32kb' }));\nconst contactSchema = z.object({ email: z.string().email().max(254), message: z.string().trim().min(10).max(2000) }).strict();\napp.post('/api/contact', requireCsrf, validateBody(contactSchema), rateLimit({ windowMs: 60_000, limit: 5 }), asyncHandler(async (request, response) => {\n  await contactService.send({ email: request.body.email, message: request.body.message });\n  response.status(202).json({ accepted: true });\n}));\n\ntest('rejects script fields', () => request(app).post('/api/contact').send({ email: 'a@b.test', message: '<script>alert(1)</script>', admin: true }).expect(400));\ntest('rejects missing csrf', () => request(app).post('/api/contact').send(validContact).expect(403));\ntest('limits oversized bodies', () => request(app).post('/api/contact').set('x-csrf-token', token).send({ ...validContact, message: 'x'.repeat(40_000) }).expect(413));", ["express.json({ limit:", "contactSchema", ".strict()", "requireCsrf", "rateLimit", "status(202)", "rejects script fields", "expect(400)", "missing csrf", "expect(403)", "expect(413)"], ["secure-forms", "input-validation", "csrf-prevention", "abuse-testing"], [v.validation, v.csrf, v.threat]),
        quiz("sec-01-review", ["Quiz : menaces et entrées", "Quiz: threats and input"], [
          q("t1", ["Quel est le point de départ d’un audit ?", "What is the starting point of an audit?"], [["Actifs, acteurs, frontières et abus", "Assets, actors, boundaries, and abuse"], ["La liste des dépendances uniquement", "Only the dependency list"], ["La couleur des boutons", "Button colors"]], "Actifs, acteurs, frontières et abus", ["Les protections doivent répondre à un risque concret.", "Controls must address a concrete risk."], ["threat-modeling"]),
          q("t2", ["Pourquoi React échappe-t-il {body} ?", "Why does React escape {body}?"], [["Le texte est rendu comme donnée, pas comme HTML actif", "Text is rendered as data, not active HTML"], ["Il chiffre le texte", "It encrypts text"], ["Il valide la session", "It validates the session"]], "Le texte est rendu comme donnée, pas comme HTML actif", ["dangerouslySetInnerHTML exige une sanitisation dédiée.", "dangerouslySetInnerHTML requires dedicated sanitization."], ["xss-prevention"]),
          q("t3", ["Quelle défense CSRF est pertinente avec cookie ?", "Which CSRF defense is relevant with cookies?"], [["SameSite, contrôle d’origine et jeton", "SameSite, origin checking, and token"], ["CORS seul", "CORS alone"], ["Masquer le bouton", "Hide the button"]], "SameSite, contrôle d’origine et jeton", ["Les défenses se complètent pour les requêtes de mutation.", "Controls complement each other for mutation requests."], ["csrf-prevention"])
        ])
      ]
    },
    {
      id: "security-identity",
      title: ["Sessions, autorisation et secrets", "Sessions, authorization, and secrets"],
      description: ["Prouver l’identité, limiter la session et contrôler chaque ressource selon le moindre privilège.", "Prove identity, limit sessions, and control each resource according to least privilege."],
      vocabulary: [v.session, v.leastPrivilege, v.secret],
      lessons: [
        code("sec-02-session", ["Durcir une session", "Harden a session"], ["Régénère après connexion, limite durée, cookie et révocation.", "Regenerate after login, limit duration and cookie, and support revocation."], "request.session.regenerate((error) => {\n  if (error) return next(error);\n  request.session.userId = user.id;\n  request.session.roles = user.roles;\n  request.session.createdAt = Date.now();\n  response.cookie('session', request.sessionID, { httpOnly: true, secure: true, sameSite: 'lax', maxAge: 30 * 60 * 1000 });\n  response.status(204).end();\n});", ["session.regenerate", "session.userId", "session.roles", "httpOnly: true", "secure: true", "sameSite: 'lax'", "maxAge", "status(204)"], ["session-security", "session-fixation"], [v.session, v.authentication]),
        code("sec-02-authorization", ["Tester l’autorisation objet par objet", "Test object-level authorization"], ["Vérifie propriétaire ou rôle sur la ressource chargée côté serveur.", "Verify owner or role on the resource loaded server-side."], "async function requireSubmissionAccess(request, response, next) {\n  const submission = await repositories.submissions.findById(request.params.submissionId);\n  if (!submission) return response.status(404).json({ error: { code: 'NOT_FOUND' } });\n  const owns = submission.userId === request.auth.userId;\n  const reviews = request.auth.roles.includes('reviewer');\n  if (!owns && !reviews) return response.status(403).json({ error: { code: 'ACCESS_DENIED' } });\n  request.submission = submission;\n  next();\n}", ["findById", "submission.userId === request.auth.userId", "roles.includes('reviewer')", "status(403)", "ACCESS_DENIED", "request.submission"], ["object-authorization", "least-privilege"], [v.authorization, v.leastPrivilege]),
        code("sec-02-secrets", ["Gérer et faire tourner les secrets", "Manage and rotate secrets"], ["Charge les secrets côté serveur, sépare environnements et prépare la rotation.", "Load secrets server-side, separate environments, and prepare rotation."], "export function loadSecrets(env) {\n  const required = ['DATABASE_URL', 'SESSION_SECRET', 'SUPABASE_SERVICE_ROLE_KEY'];\n  const missing = required.filter((name) => !env[name]);\n  if (missing.length) throw new Error(`Missing required secrets: ${missing.join(', ')}`);\n  return Object.freeze({ databaseUrl: env.DATABASE_URL, sessionSecret: env.SESSION_SECRET, serviceRoleKey: env.SUPABASE_SERVICE_ROLE_KEY });\n}\n\nexport async function rotateSigningKey(keyStore) {\n  const next = await keyStore.generate({ status: 'active' });\n  await keyStore.markPreviousAs({ status: 'verify-only', expiresAt: addHours(new Date(), 24) });\n  return next.id;\n}", ["DATABASE_URL", "SESSION_SECRET", "SUPABASE_SERVICE_ROLE_KEY", "missing.length", "Object.freeze", "rotateSigningKey", "verify-only"], ["secret-management", "key-rotation"], [v.secret, v.leastPrivilege]),
        project("sec-02-access-project", ["Mini-projet : matrice de contrôle d’accès", "Mini-project: access-control matrix"], ["Implémente rôles, propriété, tests 401/403 et refus inter-utilisateur.", "Implement roles, ownership, 401/403 tests, and cross-user denial."], "const permissions = {\n  learner: ['submission:read-own', 'submission:create'],\n  reviewer: ['submission:read-all', 'submission:review'],\n  admin: ['submission:read-all', 'submission:review', 'roles:manage']\n};\n\nfunction requirePermission(permission) {\n  return (request, response, next) => request.auth.roles.some((role) => permissions[role]?.includes(permission))\n    ? next()\n    : response.status(403).json({ error: { code: 'PERMISSION_DENIED' } });\n}\n\ntest('requires identity', () => request(app).get('/api/submissions/s1').expect(401));\ntest('denies another learner', () => request(app).get('/api/submissions/s1').set('Authorization', bearer('user-b')).expect(403));\ntest('allows reviewer', () => request(app).get('/api/submissions/s1').set('Authorization', bearer('reviewer')).expect(200));", ["const permissions", "read-own", "requirePermission", "status(403)", "requires identity", "expect(401)", "denies another learner", "expect(403)", "allows reviewer", "expect(200)"], ["rbac", "object-authorization", "security-testing"], [v.authorization, v.leastPrivilege, v.session]),
        quiz("sec-02-review", ["Quiz : identité et accès", "Quiz: identity and access"], [
          q("i1", ["Pourquoi régénérer la session après login ?", "Why regenerate the session after login?"], [["Prévenir la fixation de session", "Prevent session fixation"], ["Changer le mot de passe", "Change the password"], ["Accélérer CSS", "Speed up CSS"]], "Prévenir la fixation de session", ["Un identifiant fourni avant authentification ne doit pas survivre.", "An identifier supplied before authentication must not survive."], ["session-fixation"]),
          q("i2", ["Quelle différence entre rôle et propriété ?", "What is the difference between role and ownership?"], [["Le rôle donne une capacité, la propriété lie une ressource à l’acteur", "Role grants capability; ownership links a resource to the actor"], ["Aucune", "None"], ["La propriété est un cookie", "Ownership is a cookie"]], "Le rôle donne une capacité, la propriété lie une ressource à l’acteur", ["Les deux contrôles sont souvent nécessaires.", "Both checks are often required."], ["object-authorization"]),
          q("i3", ["Où ne jamais placer une clé service ?", "Where must a service key never be placed?"], [["Dans le frontend ou une variable VITE_", "In the frontend or a VITE_ variable"], ["Dans un coffre serveur", "In a server vault"], ["Dans le runtime backend", "In backend runtime"]], "Dans le frontend ou une variable VITE_", ["Toute variable frontend est livrée au navigateur.", "Every frontend variable is shipped to the browser."], ["secret-management"])
        ])
      ]
    },
    {
      id: "security-platform",
      title: ["CSP, CORS, uploads et dépendances", "CSP, CORS, uploads, and dependencies"],
      description: ["Durcir les protocoles et ressources autour de l’application.", "Harden protocols and resources around the application."],
      vocabulary: [v.csp, v.cors, v.upload],
      lessons: [
        code("sec-03-headers", ["Configurer CSP et en-têtes", "Configure CSP and headers"], ["Déclare une CSP restrictive et retire les capacités inutiles.", "Declare a restrictive CSP and remove unnecessary capabilities."], "app.use(helmet({\n  contentSecurityPolicy: { directives: {\n    defaultSrc: [\"'self'\"],\n    scriptSrc: [\"'self'\"],\n    styleSrc: [\"'self'\"],\n    imgSrc: [\"'self'\", 'data:'],\n    connectSrc: [\"'self'\", process.env.SUPABASE_URL],\n    objectSrc: [\"'none'\"],\n    frameAncestors: [\"'none'\"]\n  } },\n  referrerPolicy: { policy: 'strict-origin-when-cross-origin' }\n}));", ["helmet(", "contentSecurityPolicy", "defaultSrc", "scriptSrc", "objectSrc", "'none'", "frameAncestors", "referrerPolicy"], ["content-security-policy", "security-headers"], [v.csp, v.xss]),
        code("sec-03-cors-upload", ["Restreindre CORS et uploads", "Restrict CORS and uploads"], ["Autorise des origines exactes et contrôle taille, MIME, signature et nom d’objet.", "Allow exact origins and control size, MIME, signature, and object name."], "app.use(cors({\n  origin(origin, callback) {\n    if (!origin || allowedOrigins.has(origin)) return callback(null, true);\n    callback(new HttpError(403, 'CORS_ORIGIN_DENIED'));\n  },\n  credentials: true,\n  methods: ['GET', 'POST', 'PATCH', 'DELETE']\n}));\n\nasync function validateUpload(file, userId) {\n  if (file.size > 1_000_000) throw new HttpError(413, 'UPLOAD_TOO_LARGE');\n  if (!['image/png', 'image/jpeg', 'image/webp'].includes(file.mimetype)) throw new HttpError(415, 'UNSUPPORTED_MEDIA_TYPE');\n  await verifyImageSignature(file.buffer);\n  return `${userId}/${crypto.randomUUID()}`;\n}", ["allowedOrigins.has", "CORS_ORIGIN_DENIED", "credentials: true", "file.size", "UPLOAD_TOO_LARGE", "file.mimetype", "verifyImageSignature", "crypto.randomUUID"], ["cors-security", "secure-uploads"], [v.cors, v.upload]),
        text("sec-03-dependencies", ["Auditer dépendances et chaîne de build", "Audit dependencies and build chain"], ["Établis règles de lockfile, mises à jour, provenance et réaction aux vulnérabilités.", "Establish lockfile, update, provenance, and vulnerability-response rules."], ["Installation déterministe avec npm ci", "Audit production et développement séparés", "Mise à jour automatisée avec tests", "Vérification provenance et scripts d’installation", "Délai de correction selon criticité", "Inventaire SBOM et propriétaire"], ["supply-chain-security", "dependency-audit"], [v.threat, v.incident]),
        project("sec-03-hardening-project", ["Mini-projet : durcissement HTTP", "Mini-project: HTTP hardening"], ["Livre CSP, CORS, rate limits, body/upload limits et tests de refus.", "Ship CSP, CORS, rate limits, body/upload limits, and denial tests."], "app.use(helmet({ contentSecurityPolicy: { directives: { defaultSrc: [\"'self'\"], scriptSrc: [\"'self'\"], objectSrc: [\"'none'\"], frameAncestors: [\"'none'\"] } } }));\napp.use(cors({ origin: (origin, callback) => callback(null, !origin || allowedOrigins.has(origin)), credentials: true }));\napp.use(express.json({ limit: '256kb' }));\napp.use('/api/auth', rateLimit({ windowMs: 15 * 60_000, limit: 10 }));\napp.post('/api/avatar', upload.single('avatar'), validateAvatar, saveAvatar);\n\ntest('rejects evil origin', () => request(app).get('/api/catalog').set('Origin', 'https://evil.test').expect(403));\ntest('sets CSP', async () => expect((await request(app).get('/api/catalog')).headers['content-security-policy']).toContain(\"default-src 'self'\"));\ntest('rejects oversized JSON', () => request(app).post('/api/events').send({ payload: 'x'.repeat(300_000) }).expect(413));", ["helmet(", "contentSecurityPolicy", "cors(", "allowedOrigins.has", "express.json({ limit:", "rateLimit", "upload.single", "rejects evil origin", "expect(403)", "content-security-policy", "expect(413)"], ["http-hardening", "security-testing", "secure-uploads"], [v.csp, v.cors, v.upload]),
        quiz("sec-03-review", ["Quiz : plateforme durcie", "Quiz: hardened platform"], [
          q("p1", ["Que bloque object-src 'none' ?", "What does object-src 'none' block?"], [["Les plugins embarqués", "Embedded plugins"], ["Toutes les images", "All images"], ["Les requêtes API", "API requests"]], "Les plugins embarqués", ["La directive retire une surface historique rarement nécessaire.", "The directive removes a rarely needed legacy surface."], ["content-security-policy"]),
          q("p2", ["CORS authentifie-t-il l’utilisateur ?", "Does CORS authenticate the user?"], [["Non, il contrôle seulement certaines lectures navigateur inter-origines", "No, it only controls some cross-origin browser reads"], ["Oui, toujours", "Yes, always"], ["Seulement avec GET", "Only with GET"]], "Non, il contrôle seulement certaines lectures navigateur inter-origines", ["Auth et autorisation restent obligatoires côté serveur.", "Server-side auth and authorization remain mandatory."], ["cors-security"]),
          q("p3", ["Pourquoi vérifier la signature d’un fichier ?", "Why verify a file signature?"], [["Le MIME déclaré peut être falsifié", "Declared MIME can be spoofed"], ["Pour changer son nom", "To change its name"], ["Pour éviter HTTPS", "To avoid HTTPS"]], "Le MIME déclaré peut être falsifié", ["Le contenu doit correspondre au format autorisé.", "Content must match the allowed format."], ["secure-uploads"])
        ])
      ]
    },
    {
      id: "security-incident",
      title: ["Logs, incident et audit final", "Logs, incident, and final audit"],
      description: ["Détecter sans collecter de secrets, contenir un incident et prouver les corrections.", "Detect without collecting secrets, contain an incident, and prove fixes."],
      vocabulary: [v.incident, ["journal structuré", "structured log", "Événement machine lisible avec champs stables.", "A machine-readable event with stable fields."], ["preuve négative", "negative evidence", "Test démontrant qu’une action interdite est refusée.", "A test proving a forbidden action is denied."]],
      lessons: [
        code("sec-04-logging", ["Journaliser sans secrets", "Log without secrets"], ["Émets contexte, décision et durée tout en expurgeant tokens, cookies et bodies sensibles.", "Emit context, decision, and duration while redacting tokens, cookies, and sensitive bodies."], "const logger = pino({ redact: ['req.headers.authorization', 'req.headers.cookie', 'password', '*.token', '*.secret'] });\n\nresponse.on('finish', () => logger.info({\n  requestId: request.id,\n  method: request.method,\n  path: request.route?.path || request.path,\n  status: response.statusCode,\n  durationMs: Date.now() - request.startedAt,\n  userId: request.auth?.userId || null\n}, 'request completed'));", ["pino({ redact:", "authorization", "cookie", "requestId", "status:", "durationMs", "userId", "request completed"], ["secure-logging", "request-tracing"], [v.incident, v.observability]),
        text("sec-04-response", ["Conduire une réponse à incident", "Run incident response"], ["Rédige les étapes d’un vol de clé service sans détruire les preuves.", "Write the steps for a service-key theft without destroying evidence."], ["Détecter et ouvrir un incident horodaté", "Révoquer et faire tourner la clé compromise", "Limiter les accès et conserver logs immuables", "Identifier les opérations réalisées", "Notifier selon impact et obligations", "Corriger la cause puis tester", "Documenter chronologie et actions préventives"], ["incident-response", "secret-rotation"], [v.incident, v.secret]),
        project("sec-04-capstone", ["Projet final : audit et remédiation", "Final project: audit and remediation"], ["Livre modèle de menace, risques priorisés, correctifs, tests négatifs et plan d’incident.", "Ship a threat model, prioritized risks, fixes, negative tests, and an incident plan."], "# Audit sécurité PulsaTeach\n\n## Actifs et frontières\n- Identités, progression, quiz privés, certificats, clés service\n- Navigateur → API → PostgreSQL/Supabase → email\n\n## Risques prioritaires\n1. Accès inter-utilisateur aux résultats\n2. Publication sans rôle reviewer\n3. Upload actif ou surdimensionné\n4. Secret livré au frontend\n\n## Correctifs\n- Auth et autorisation objet côté serveur\n- Schémas stricts et limites de payload\n- CSP, CORS explicite, rate limiting\n- RLS sur données privées\n- Logs structurés avec redaction\n\n## Preuves\n- Tests 401/403 et accès croisé\n- Payloads inconnus rejetés 400\n- Origine hostile rejetée 403\n- Upload > 1 MB rejeté 413\n- Scanner de bundle sans secret\n\n## Incident\n- Détection, rotation, confinement, analyse, notification, post-mortem", ["Actifs et frontières", "Accès inter-utilisateur", "Auth et autorisation", "Schémas stricts", "CSP", "RLS", "Tests 401/403", "rejetée 413", "Scanner de bundle", "rotation", "post-mortem"], ["security-audit", "threat-modeling", "remediation", "security-testing", "incident-response"], [v.threat, v.incident, v.leastPrivilege], true),
        quiz("sec-04-review", ["Quiz : incident et preuve", "Quiz: incident and evidence"], [
          q("l1", ["Que ne faut-il jamais journaliser ?", "What must never be logged?"], [["Tokens, mots de passe et secrets", "Tokens, passwords, and secrets"], ["requestId", "requestId"], ["statut HTTP", "HTTP status"]], "Tokens, mots de passe et secrets", ["La redaction doit être configurée avant l’incident.", "Redaction must be configured before an incident."], ["secure-logging"]),
          q("l2", ["Première action après confirmation d’une clé volée ?", "First action after confirming a stolen key?"], [["Révoquer/rotater et limiter l’accès en conservant les preuves", "Revoke/rotate and limit access while preserving evidence"], ["Supprimer tous les logs", "Delete every log"], ["Attendre une semaine", "Wait a week"]], "Révoquer/rotater et limiter l’accès en conservant les preuves", ["Le confinement réduit l’impact sans effacer la chronologie.", "Containment reduces impact without erasing the timeline."], ["incident-response"]),
          q("l3", ["Quelle preuve valide une autorisation ?", "What evidence validates authorization?"], [["Un test inter-utilisateur refusé 403", "A cross-user test denied with 403"], ["Un bouton caché", "A hidden button"], ["Une phrase dans README", "A sentence in README"]], "Un test inter-utilisateur refusé 403", ["Le comportement serveur doit être observé directement.", "Server behavior must be observed directly."], ["security-testing"])
        ]),
        quiz("security-final-exam", ["Examen Sécurité web", "Web security exam"], [
          q("x1", ["Qu’est-ce qu’une défense en profondeur ?", "What is defense in depth?"], [["Plusieurs contrôles indépendants autour d’un risque", "Several independent controls around a risk"], ["Un seul middleware très long", "One very long middleware"], ["Une checklist sans test", "A checklist without tests"]], "Plusieurs contrôles indépendants autour d’un risque", ["Validation, autorisation, RLS et logs couvrent des défaillances différentes.", "Validation, authorization, RLS, and logs cover different failures."], ["threat-modeling"]),
          q("x2", ["Comment traiter du HTML utilisateur ?", "How should user HTML be handled?"], [["Éviter l’interprétation ou sanitiser avec une politique dédiée", "Avoid interpretation or sanitize with a dedicated policy"], ["L’insérer directement", "Insert it directly"], ["Le cacher en CSS", "Hide it with CSS"]], "Éviter l’interprétation ou sanitiser avec une politique dédiée", ["L’encodage texte est préférable lorsque le HTML n’est pas nécessaire.", "Text encoding is preferable when HTML is unnecessary."], ["xss-prevention"]),
          q("x3", ["Que signifie moindre privilège ?", "What does least privilege mean?"], [["Permissions minimales et temporaires", "Minimal and temporary permissions"], ["Tous les rôles pour éviter les blocages", "Every role to avoid blockers"], ["Aucun contrôle interne", "No internal controls"]], "Permissions minimales et temporaires", ["Réduire les capacités réduit l’impact d’un compromis.", "Reducing capabilities reduces compromise impact."], ["least-privilege"]),
          q("x4", ["Pourquoi RLS complète-t-elle l’API ?", "Why does RLS complement the API?"], [["Elle protège les lignes si une route oublie un filtre", "It protects rows if a route misses a filter"], ["Elle remplace HTTPS", "It replaces HTTPS"], ["Elle valide le CSS", "It validates CSS"]], "Elle protège les lignes si une route oublie un filtre", ["La base applique une seconde frontière indépendante.", "The database applies a second independent boundary."], ["row-level-security"]),
          q("x5", ["Quand un audit est-il terminé ?", "When is an audit complete?"], [["Quand chaque risque prioritaire possède correction et preuve reproductible", "When every priority risk has a fix and reproducible evidence"], ["Quand axe est vert", "When axe is green"], ["Quand aucune erreur console n’apparaît", "When no console error appears"]], "Quand chaque risque prioritaire possède correction et preuve reproductible", ["L’absence de signal automatique ne prouve pas l’absence de risque.", "Lack of automated signal does not prove lack of risk."], ["security-audit"])
        ], "exam", 80)
      ]
    }
  ]
});

function code(id, title, brief, solution, requirements, skills, vocabulary) {
  return { id, type: "node", title, brief, solution, requirements, skills, vocabulary };
}

function text(id, title, brief, requirements, skills, vocabulary) {
  return { id, type: "text", title, brief, solution: requirements.map((item) => `- ${item}`).join("\n"), requirements, skills, vocabulary };
}

function project(id, title, brief, solution, requirements, skills, vocabulary, finalProject = false) {
  return {
    id,
    project: true,
    exerciseType: solution.trim().startsWith("#") ? "text" : "node",
    title,
    brief,
    solution,
    requirements,
    skills,
    vocabulary,
    durationMin: finalProject ? 240 : 120,
    xp: finalProject ? 160 : 95
  };
}

function quiz(id, title, questions, purpose = "module-review", passingScore = 70) {
  return { id, type: "quiz", title, questions, purpose, passingScore, brief: ["Choisis la défense qui répond au risque observé.", "Choose the defense that addresses the observed risk."] };
}

function q(id, prompt, options, answer, explanation, skills) {
  return {
    id,
    type: "single",
    prompt,
    choices: options.map((option) => ({ id: option[0], label: option })),
    answer,
    explanation,
    points: 1,
    skills,
    glossaryTerms: []
  };
}
